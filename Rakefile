desc "Opcion por defecto: ejecutar tests"
task :default => :tests

desc "Iniciar servidor"
task :server do
  sh "./manage.py runserver"
end

desc "Iniciar servidor"
task :s do
  sh "./manage.py runserver"
end

desc "Ejecutar tests"
task :test do
  sh "./manage.py harvest"
end

desc "Ejecutar tests"
task :tests do
  sh "./manage.py harvest"
end

desc "Actualizar BBDD"
task :db do
  sh "./manage.py syncdb"
end
